# Roster Import — RA An Nisaa' Full Roster (TA 25/26)

## Context

School admin (shanti) sent `Data siswa TA 2526 (REupdated).xlsx` — 140 students across 13 kelas (DC, TD1, TD2, KB1, KB3, KB4, A1-4, B1-4), described as "udh gabungan murid baru dan lama" (combined new + returning students) for RA An Nisaa'. Per shanti: NIS's first 4 digits encode the academic year the student first enrolled (e.g. `2526137301` → entered TA 25/26); daycare infants (`DC M`) carry no NIS since they aren't formally enrolled yet.

This is separate from the [2026-07-13 roster import](2026-07-13-roster-import-2026-2027.md), which put 34 students into prod under a different structure (campuses "Taman Aster" / "Metland Cibitung", classes KB/TK A/TK B). Initial assumption was this file might be a superset of those 34 — investigation (direct prod SQL cross-reference on name, then on parent name + address) found **zero duplicate students**, but **3 sibling-family overlaps** (same parents, different child) that would create duplicate `Parent` rows if guardian dedup is skipped: Frengki Kurniawan + Umi Kulsum (prod: Aliza Zhafira Kurniawan / xlsx: Arsen Zhafier Kurniawan), Andri Pambudi + Isnainni Choirunnisa (prod: Shreya Annaya Maheswari / xlsx: Lathisa Deana Maheswari), Deni Irawan + Atik Dwi Kristanti (prod: Denisha Putri Irawan / xlsx: Rafisqy Alfarezi Irawan).

No `ClassSection` exists yet for any of this file's 13 kelas names — prod currently only has the 6 Aster/Metland sections from the prior cycle. The file has no campus column, so which physical campus (or a 3rd campus) these kelas belong to is unknown, as is which `AcademicYear`/`Semester` this import should attach to (sheet headers show stale, inconsistent labels — mostly "2024/2025", some "2025/2026").

**Operational pattern (owner's observation):** shanti's workflow appears to be copy-forward per kelas each year — a student promoted from KB1 to A1 the next year gets their row copied into the new kelas sheet, keeping the same NIS (assigned once at entry, per shanti's own explanation of the NIS-year-prefix rule). This explains the mixed 2024/2025 vs 2025/2026 sheet headers — it's staleness from which sheets she's edited this pass, not a real multi-year ambiguity. It also means **this file will recur** — next year's roster file will re-send most of these same 140 students (promoted a kelas, same NIS). Any import must be idempotent keyed on NIS: re-running against a future file should update/skip already-imported students, not duplicate them. This is a hard requirement for T2, not just a nice-to-have.

## Spec

Import RA An Nisaa's existing roster into prod, correctly enrolled and de-duplicated against the already-imported 34 students, such that:
- Every student in the 13 kelas has a `Student` row, correct `ClassSection` enrollment, and father+mother `Parent` guardians.
- The 3 sibling families reuse their existing `Parent` row (matched by name — verify against phone/NIK if available) rather than creating duplicates.
- Data gaps are resolved with shanti, not silently guessed.

Acceptance criteria:
- [x] Campus mapping confirmed for 11 of 13 kelas — Aster: TD1, KB1, A1, A2, B1, B2. Metland: TD2, KB3, KB4, A3, A4, B3, B4. **Still open:** owner's mapping also named `DC1`/`DC2`/`KB2`, but the file has only one `DC` sheet (11 students, not split) and no `KB2` sheet at all — need shanti to clarify whether `DC` is one of DC1/DC2/combined, and whether KB2 exists this year (separate roster to send, or genuinely doesn't exist).
- [x] Target `AcademicYear` resolved as **TA 2025/2026**: shanti's message excludes Fahreza Arkha Bima (A1) from this roster specifically because he "belum sempat sekolah dan masuk di tahun ajaran 2026/2027" — she's actively distinguishing this file (TA2025/2026) from next year's cohort (TA2026/2027), confirming this import targets the former. Exact `Semester` boundary dates within TA2025/2026 still need picking (not yet asked — low-risk, standard Jul–Dec/Jan–Jun split per the prior cycle's pattern).
- [ ] `ClassSection` rows created for all 13 kelas under the confirmed campus/AY (triggers `reconcileSessions` per prior cycle's pattern)
- [x] TD1 gap resolved — not missing data, real: only 1 student ever, withdrew after 3 months (pindah domisili luar kota) with intentionally incomplete data. Decision: import as `status: WITHDRAWN` with the partial fields available (name, nis, gender) — no guardian/address chase, same precedent as Ghiani Harahap's exclusion in the prior cycle but here imported (not excluded) since a real NIS + withdrawal fact exists.
- [x] NIS digit-length inconsistency confirmed as **not an error**: 9-digit NIS = KB level (fewer cumulative historical enrollees), 10-digit = TK level (thousands, counted from program start). First-4-digit year-prefix rule holds for both lengths — no normalization needed, accept both as valid.
- [x] Blank-NIS rows resolved for 5 of 6, shanti confirmed directly:
  - Muhammad Ibrahim (KB1) → `252632427` (matches the ledger cross-ref).
  - Fahreza Arkha Bima (A1) → **exclude from this import** — hasn't attended, enters TA2026/2027 (a future cohort, out of scope for this cycle).
  - Muhammad Ghaisan Keenandra Ramadhika (B1) → attended a few months then withdrew; NIS may exist handwritten on a printout, not yet in Excel. Import as `status: WITHDRAWN`, `nis: null` for now — same TD1-style precedent (real withdrawn kid, partial data).
  - Sholeh Nabil Razzaaq (B1) → never attended, withdrew before starting — **exclude entirely**, same precedent as Ghiani Harahap in the prior cycle (never actually enrolled).
  - Hasna Azzahra (DC) → `-` is intentional: daycare, not yet schooling (an exception case like `DC M`, not a data gap). Shanti checking with the daycare head tomorrow for her form — **defer**, exclude from this import pass.
  - Linaya Aqila Zalfa (DC) — **still unaddressed**, not mentioned in shanti's reply. Only remaining open item in this bucket.
- [ ] 3 sibling families reuse existing prod `Parent` rows — verified count: 0 new duplicate parents for those 6 people
- [ ] Post-import: `Student` count increases by exactly (140 − TD1 gap − any still-unresolved rows), `StudentGuardian` count reflects shared-parent dedup, per-kelas enrollment counts match source sheet counts
- [ ] Import is NIS-keyed idempotent: re-running the same (or a future year's) file against prod updates/skips students whose NIS already exists rather than creating duplicates

Non-goals: DC M (daycare infants, explicitly no NIS/no formal enrollment per shanti — excluded entirely), parent portal accounts (deferred per pilot rollout project), reconciling the 2024/2025-vs-2025/2026 label inconsistency in the source file itself (shanti's file, not ours to edit), NISN validation/lookup (imported as-is if present), Fahreza Arkha Bima (belongs to TA2026/2027, not this import), Sholeh Nabil Razzaaq (never enrolled, withdrew before starting).

Assumptions I'm making (correct me or confirm before /build):
1. This file supersedes/extends the prior 34-student import rather than replacing it — both cohorts stay in prod side by side, no rollback of the prior cycle.
2. Sibling dedup is matched by (father name + mother name) exact match against existing `Parent.name` — good enough for these 3 confirmed cases, but a stronger key (NIK) should be used if the xlsx NIK values line up with prod once compared directly.
3. TD1's near-empty data sheet is a genuine gap in what shanti sent, not a parsing miss on my part (verified: no `Data TD1` sheet exists in the workbook, only the attendance-only `TD1` sheet with 1 name) — **confirmed by shanti**: real single-student class, not a missing-data gap.

## Tasks

- [ ] **T1 (blocked on shanti):** Campus mapping, TD1, NIS digit-length, target AY, and 5 of 6 blank-NIS rows resolved (see Spec). Still need: DC1/DC2/KB2 clarification, Linaya Aqila Zalfa's NIS (only remaining unaddressed blank), Hasna Azzahra's daycare form (shanti checking with daycare head), M. Ghaisan's real NIS from the printout (currently importing with `nis: null`, acceptable per spec but a real value is better if it turns up).
- [x] T2: Build the field-mapping + guardian-dedup + NIS-keyed idempotent import script. Acceptance met: dry-run structure computes correct student/guardian counts with sibling families resolved to existing `Parent.id`s via strict ayah+ibu pair matching (not lone-name matching), zero new duplicates by construction, and NIS-keyed skip logic makes a second run against the same file produce zero new inserts. Not yet exercised against live prod data (blocked on T1's remaining opens: DC1/DC2/KB2, target AY, 5 blank-NIS rows) — script fails loudly on a missing target AcademicYear rather than guessing.
- [ ] T3: Execute the import against prod using `scripts/roster-import/run.ts --commit` (Prisma-direct, not the admin API — see Implementation note below for why this diverges from the prior cycle's approach). Acceptance: SQL verification shows `Student` count, `StudentGuardian` count, and per-kelas `StudentEnrollment` counts match the confirmed source counts from T1.

## Implementation

- Subagent plan: driver=claude-sonnet-5, dirty-work=claude-sonnet-5 (single general-purpose subagent — cycle too small to split T2 further; driver reviewed + sent back one fix-round rather than doing a parallel fan-out).
- Task T2: `scripts/roster-import/{parse-xlsx,map-fields,dedupe,config,run}.ts` + `scripts/roster-import/__tests__/*.test.ts` — Prisma-direct idempotent import script, dry-run by default. Architecture note: deviates from the prior cycle's "drive the admin API from the browser" approach — this repo's own `scripts/reseed-staging.ts` / `scripts/reseed/people.ts` precedent uses Prisma directly for bulk data ops, and `reconcileSessions` (the side effect the API route triggers on `ClassSection` create) is a plain importable function from `lib/sessions/reconcile.ts`, not something locked behind the HTTP layer. Chosen because this import recurs yearly (140 vs the prior one-off's 35) and a committed, testable script is worth it here.
  - `config.ts` hardcodes the 11-of-13 resolved campus mapping; `DC` and `KB2` deliberately absent — the script warns and skips any kelas not in the map rather than guessing.
  - `dedupe.ts`: parent reuse requires the record's ayah+ibu names to match an existing family's ayah+ibu pair *together* (not either name independently) — a code-review pass caught the independent-match version as a blocker (risk: two unrelated families sharing one common first name get cross-wired).
  - `run.ts --commit` requires `--academic-year=` explicitly and throws rather than creating/guessing an `AcademicYear` — T1's AY question is still open.
  - Each student's writes (Parent create-or-reuse, Student, StudentGuardian, StudentEnrollment) run inside one `prisma.$transaction` so a mid-import failure can't leave an orphaned half-created Student that idempotency would then permanently skip.
  - Error logs redact PII (NIS/kelas/row index only, never raw NIK/address/income) per code review.
  - Code review (`superpowers:code-reviewer`) found 3 blockers pre-fix (parent dedup independent-name matching, missing primary-guardian fallback for mother-only rows, no per-student transaction) — all fixed and re-verified independently (build + scoped vitest run) before commit, not just trusted from the subagent's self-report.
  - Bonus catch while writing `parse-xlsx.ts` tests: header-row detection was anchoring on the literal text "NIS", which appears in both the super-header and sub-header rows of the real sheets — was silently picking the wrong row. Fixed by anchoring on "Lengkap" instead (unique to the true sub-header). Re-verified against the real source file post-fix.

## Verification

- Task T2: `npm run build` — clean. `npx vitest run` — full suite 2187 passed / 42 todo (no regressions); scoped `scripts/roster-import` suite 38 passed (3 files) — independently re-run by the driver, not just taken from the subagent's report, per this project's "verify subagent test claims" rule.
- Playwright: not run this task — no application code changed, `scripts/roster-import/**` has no UI/route surface. Deferred to end-of-cycle per the doc-only/non-UI nature of this change; will reassess once T3 (real prod writes) lands.

## Ship Notes
