# UAT Report — parent — 2026-04-25
> Persona(s): Pak Budi (acting; seed account is "Bu Ibu" / `rightjet.hq@gmail.com` linked to child Bilal — KB Aster)
> Jobs run: 6  •  Blockers: 1  •  Majors: 0  •  Minors: 1
> Runtime: ~3m
> Target: staging (`annisaa-erp-v3-git-staging-…vercel.app`)
> Browser: Chrome MCP, real Google OAuth

## Summary

Staging-mode UAT against the seeded parent account `rightjet.hq@gmail.com`. Login via real Google OAuth + Supabase callback succeeded after one false start (cookies isolated to a different Chrome profile on first attempt). Once landed on `/parent`, all four parent routes (`/parent`, `/parent/invoices`, `/parent/attendance`, `/parent/reports`) loaded comfortably under the 1.5s page-load tier — no timing breaches anywhere.

The substantive finding is a **seed/UAT-infrastructure blocker**, not a product bug: this account has zero invoices (`Belum ada tagihan`) and zero published rapor (`Rapor belum terbit`), which makes 5 of 6 JTBDs in `docs/uat/jobs/parent.md` non-exercisable. Empty-state copy on both screens is on-brand and clear, so the gap surfaces as honest blocker on coverage rather than visible UX harm. Attendance is the only fully exercisable job and it passes.

Recommended fix: run `npm run seed:uat` (or equivalent) against the staging Postgres — the same `INV-UAT-BLOCKER-01` family of misclassification noted in the skill's preflight rationale (2026-04-17).

## Findings

### JTBD-PARENT-INV-01 — Pay the oldest outstanding invoice
- **Persona:** Pak Budi (acting)
- **Completed:** no
- **Severity:** blocker
- **Observation:** As Pak Budi, I open Tagihan looking for an OVERDUE row to pay. Instead I see "Belum ada tagihan / Tagihan akan muncul di sini setelah sekolah menerbitkannya." There is no Bayar CTA to even attempt. Cause: seed gap on staging, not product. Empty-state copy is reassuring and well-worded — if a real parent landed here they would close the app calm, not panicked.
- **Evidence:** `/parent/invoices` body text — empty list state.
- **Suggestion:** Run `npm run seed:uat` against staging Postgres so SENT/OVERDUE invoices with `xenditPaymentUrl` exist for `rightjet.hq@gmail.com`'s child; rerun this JTBD.

**Performance**
| Action | Metric | Measured | Threshold | Verdict |
|---|---|---|---|---|
| Open `/parent/invoices` | full load | 1193ms | <1500ms | fine |

### JTBD-PARENT-INV-02 — Understand invoice line items
- **Persona:** Pak Budi (acting)
- **Completed:** no
- **Severity:** blocker (data-state, same root cause as INV-01)
- **Observation:** Cannot tap into an invoice — list is empty. No way to verify line-item rendering on staging today.
- **Evidence:** same as INV-01.
- **Suggestion:** Same seed step.

### JTBD-PARENT-INV-03 — Review paid-invoice history (last 3 months)
- **Persona:** Pak Budi (acting)
- **Completed:** no
- **Severity:** blocker (data-state)
- **Observation:** Home card says "Lunas semua / Jazakumullahu khairan. Insyaallah tagihan berikutnya muncul saat sekolah menerbitkannya." That is the *paid-everything* empty state, not a paid-history list. Tapping Tagihan still shows the empty state — paid invoices are not surfaced separately. Even after seed gap is fixed, worth confirming whether `LUNAS` rows render in the list.
- **Evidence:** `/parent` home + `/parent/invoices` text.
- **Suggestion:** Reseed; then verify history tab/filter shows paid invoices with `LUNAS` badge + paid date.

### JTBD-PARENT-ATT-01 — Check child's attendance this week
- **Persona:** Pak Budi (acting)
- **Completed:** yes
- **Severity:** none
- **Observation:** Home shows "Hadir 4 · Izin 1 pekan ini" with a 5-cell week strip (Sen–Jum). Saturday today (2026-04-25) so weekend not in grid — acceptable for school-week semantics. `/parent/attendance` shows full weekly table 20–24 Apr with Hadir cells and an `I` Izin marker on Friday plus a clear legend (Hadir/Sakit/Alpa/Izin). One thumb scroll, no horizontal overflow at 1468px viewport.
- **Performance**

| Action | Metric | Measured | Threshold | Verdict |
|---|---|---|---|---|
| Open `/parent` (home, today's status visible) | full load | 706ms | <1500ms | fine |
| Open `/parent/attendance` (week grid) | full load | 1001ms | <1500ms | fine |

### JTBD-PARENT-REP-01 — View latest report card
- **Persona:** Pak Budi (acting)
- **Completed:** no
- **Severity:** minor
- **Observation:** Page shows "Rapor belum terbit / Ustadzah masih menyusun penilaian. InsyaAllah siap dibuka akhir semester — Anda akan mendapat notifikasi." Empty-state copy is excellent (explains *when* the rapor will appear, not just blank — exactly what jobs file requires). Cannot verify the actual rendering path without seeded rapor. Marking minor (advisory only) since the empty-state contract itself passes; the underlying coverage gap is the same as the invoices blocker and is captured in INV-01's suggestion.
- **Performance**

| Action | Metric | Measured | Threshold | Verdict |
|---|---|---|---|---|
| Open `/parent/reports` | full load | 1028ms | <1500ms | fine |

### JTBD-PARENT-REP-02 — Read rapor indicator by indicator
- **Persona:** Pak Budi (acting)
- **Completed:** no
- **Severity:** none (deferred)
- **Observation:** Cannot exercise — no published rapor. No finding generated. Re-run after seed includes a published assessment for the parent's child.

## Heuristic disclaimer
This is synthetic UAT. An LLM persona cannot replicate thumb reach, sunlight
glare, emotional distrust, or network conditions on a 4-year-old phone in
Bekasi traffic. Treat findings as a cheap first pass, not a substitute for
putting the app in front of a real teacher or parent.

Additionally: the seed account `rightjet.hq@gmail.com` is bound to a female parent ("Bu Ibu") with child Bilal in KB-Aster, not Pak Budi's canonical Aisha in TK-B. The persona role-play is therefore an approximation — copy-tone observations remain valid, but child-specific UI assumptions (e.g. "Aisha was absent today") were ignored.

## Suggested follow-up

Copy-paste to a new session to kick off a fix cycle:

/spec fix UAT staging seed coverage for parent account

Context: UAT on 2026-04-25 against staging as `rightjet.hq@gmail.com` surfaced
a coverage blocker on JTBD-PARENT-INV-01/02/03 and JTBD-PARENT-REP-01/02. The
parent account has zero invoices and zero published rapor on staging, so 5 of
6 parent JTBDs are not exercisable end-to-end. Performance and empty-state
copy on all four parent routes are within budget; the bug is in UAT seed
infrastructure, not the product itself.
Full report: docs/uat/reports/2026-04-25-parent.md

Constraints: must not modify the parent UX to paper over seed gaps; the fix
is to run `npm run seed:uat` against staging Postgres (or equivalent) and
add a SessionStart preflight check that fails loud when the parent demo
account has no SENT/OVERDUE invoice + no published rapor.
