# PR #526: update input-otp

## Context

The user approved work on existing dependency PR #526 only, against staging `deb0f035163c7bff2a3405a5c8f28c66eea060d9`. Its package and lockfile changes update `input-otp` from `1.4.2` to `1.5.0`. The app contains a shared `components/ui/input-otp.tsx` wrapper, but independent consumer review confirmed it has no active app consumer or auth-path impact. Keep this dependency-only classification unless later verification provides contrary evidence.

## Spec

- Review and verify only PR #526's `input-otp` dependency update.
- Preserve the shared wrapper API, app behavior, and all required CI checks; make no production changes.
- Reclassify and use signed-in preview verification if review finds an auth flow, callback, session, cookie, or guard impact. Do not assume that an unused wrapper settles the question before the consumer review completes.
- This changes a UI dependency, so it is not a docs-only route. Use local component/browser verification; Google verification is not required unless the auth-impact review changes the classification.

## Tasks

- [x] Confirm the PR changes only `input-otp` and its lockfile resolution relative to the approved base.
- [x] Trace the shared wrapper and package changes for app/auth consumers and upstream behavior changes.
- [x] Complete local install, typecheck, lint, build, and unit tests.
- [x] Run Playwright E2E and compare the soft-skip count with baseline.
- [x] Verify the actual OTP component in the standalone local browser harness and record evidence.
- [x] Complete the documentation audit.
- [x] Prepare the exact-head CI and current-base/head merge guard.

## Implementation

- Subagent plan: driver `gpt-6`; delegate dependency and upstream audit plus docs/browser review to `gpt-6-luna`, then independently review the evidence.
- The current diff changes `input-otp` from `^1.4.2` to `^1.5.0` in `package.json` and updates its lockfile record. No application source changes are currently part of the PR.
- Independent consumer review confirmed the shared wrapper is unused by active app code and found no auth impact. A temporary esbuild harness exercised the actual wrapper and installed `input-otp` 1.5.0 without app source edits.

## Verification

- Current candidate head: `f2afa7142536826f75ee6c1003b026b71e8a0971`; its tree `c0293b8db7e2f6ccfd4978d3d84d4b6ccd66b371` exactly matches source candidate `13cc132eaad6cea436ac5bc2ff307335f7f949d2` (zero diff after a concurrent Dependabot rebase). Approved staging base: `deb0f035163c7bff2a3405a5c8f28c66eea060d9`.
- Independent upstream review of the [input-otp v1.5.0 release](https://github.com/guilhermerodz/input-otp/releases/tag/v1.5.0) found backwards-compatible stable fixes. The experimental iOS-selection workaround and narrowed `onComplete` behavior were excluded/reverted from v1.5.0. React peer ranges are unchanged, including React 19.2.8.
- Local portable gates passed: `npm ci`; `npm run typecheck`; `npm run build`; `npm run lint` (0 errors, 59 existing warnings); `npm test` (342 suites passed, 2 skipped; 3,314 tests passed, 42 todo). Logs: `/private/tmp/annisaa-pr-tools/pr526-install.log`, `pr526-typecheck.log`, `pr526-build.log`, `pr526-lint.log`, and `pr526-unit.log`.
- `npx playwright test` passed 145 tests with 8 existing skips in 3.3 minutes. The soft-skip count stayed at 31 (baseline 31, current 31; delta 0). Log: `/private/tmp/annisaa-pr-tools/pr526-e2e.log`.
- Standalone browser harness at `http://localhost:3026/` loaded the actual `components/ui/input-otp.tsx` wrapper and installed `input-otp` 1.5.0. Typing `123456` filled all six slots and called `onComplete` with `123456`; End then Backspace produced `12345`, and typing `9` produced `123459` with the matching callback. A synthetic `ClipboardEvent` with the documented `pasteTransformer` path produced `908172` and called `onComplete`. This verifies the explicit transformer/manual paste path only; native Ctrl/Meta+V was not verified because headless clipboard reads were denied, and iOS/mobile paste was not tested.
- Remount behavior is inconclusive: a conditional unmount/remount attempt produced duplicate input nodes in the harness accessibility snapshot, so no remount pass or application defect is claimed. Disabled/error variants were not exercised. No application exceptions or console errors occurred; the local document and bundle returned HTTP 200/304. An incidental `/favicon.ico` request returned 404; there were no remote/API requests. The scratch harness styling is not full application design-system visual verification, and no app page consumes this wrapper. Screenshot: [six-digit OTP input](screenshots/pr-526-input-otp/otp-six-digit.png).
- The exact-head CI and current-base/head merge guard is prepared, but required CI remains pending until the evidence commit is published. Verify every required check on the exact published head and recheck the current base/head before merge.
- Documentation audit: `bash scripts/audit-docs.sh --write` reported 13 ok, one existing ADR-age warning, and 0 fail; `git diff --check` passed.
- The local browser work is isolated component evidence, not signed-in preview verification.
- The completed consumer/upstream review found no auth impact. If later verification provides contrary evidence, stop the local-only route and require signed-in preview verification before merge.

## Ship Notes

No production change is authorized. Keep the existing PR scope and require all four protected CI checks—`Docs sync`, `Lint, Typecheck & Test`, `Build`, and `Playwright E2E`—plus any other required checks to succeed on the exact current head before merging to staging. Recheck current base/head immediately before merge. Roll back by reverting the dependency PR.
