# env files — NEVER commit these
.env
.env.local
.env.*.local

# Next.js
.next/
out/

# Node
node_modules/

# OS
.DS_Store

# Prisma
prisma/migrations/*.sql.bak
lib/generated/prisma

# SQLite dev database
dev.db
dev.db-journal
prisma/dev.db

# Vitest
coverage/

# IDE
.vscode/
.idea/
.claude/worktrees/
.worktrees/

# Session-local state (written by the assistant on turn 1, per-session)
.claude/session-role
.githooks/.installed

# Build / analysis artifacts
build.log
build-analyze.log
tsconfig.tsbuildinfo

# Reference material (large xlsx, third-party repos, etc.) — kept locally, never committed
artifacts/

# Third-party agent skill plugins (loaded from ~/.claude/, not this repo)
.agents/

# Playwright output
playwright-report/
test-results/

# UAT reports — written by /uat, committed only when a /spec cycle consumes them
# (the consuming cycle uses `git add -f` to stage the specific report into its commit)
docs/uat/reports/

