# UAT Report — parent — 2026-04-18
> Persona(s): Pak Budi (role-played against live session as "Ibu Nurul", child Bilal Hakim, KB Metland)
> Jobs run: 3  •  Blockers: 2  •  Majors: 2  •  Minors: 0
> Runtime: ~8 min
> Environment override: run against **staging Vercel deployment** (not local `DEMO_MODE=true npm run start`) and driven through the user's **active Chrome session** (real SSO login, not demo-cookie Playwright). Timings therefore include Vercel edge/cold-start reality, which is arguably more faithful to production than the local skill default.

## Summary
Invoice payment is still blocked — the invoice detail dialog for `INV-2026-0201` shows `"Link pembayaran sedang disiapkan. Silakan coba lagi dalam beberapa saat."` with **no "Bayar" CTA**, only a "Tutup" button. This is the same blocker that `docs/uat/jobs/parent.md` claims was resolved on 2026-04-17 in cycle `parent-uat-fixes`; the resolution either did not ship to staging or regressed (most likely the staging DB was not re-seeded via `POST /api/admin/seed` after the seed change, so `xenditPaymentUrl` is missing on the existing invoice rows). Page performance is a second, independent problem: every parent route measures in the major-or-worse band on a warm Vercel staging endpoint — `/parent/reports` breaches the 4s blocker threshold at 5.3s full load. Attendance and reports drill-downs themselves work and communicate clearly.

## Findings

### JTBD-PARENT-INV-01 — Pay oldest outstanding invoice
- **Persona:** Pak Budi
- **Completed:** no
- **Severity:** blocker
- **Observation:** Home card and invoices list both surface INV-2026-0201 (Rp 475.000, "Belum Dibayar") clearly — that part works. Tapping "Lihat" opens a full-screen invoice dialog that shows the breakdown (SPP Bulanan Rp 400.000 + Kegiatan & Ekskul Rp 75.000) and — instead of a payment button — a neutral info banner: `"Link pembayaran sedang disiapkan. Silakan coba lagi dalam beberapa saat."` The only action available is "Tutup". As Pak Budi on the Commuter line, I have literally no path forward to actually pay. Worse, the copy implies "try again later" with no ETA — he would tap Tutup, re-open, see the same message, then decide to hit the bank transfer channel instead and stop trusting the portal.
- **Evidence:** Dialog text captured in-session: `"INV-2026-0201 · Belum Dibayar · Bilal Hakim · April 2026 · KB Metland · Kelompok Bermain · Link pembayaran sedang disiapkan. Silakan coba lagi dalam beberapa saat. · Jatuh Tempo: 15 Apr 2026 · RINCIAN TAGIHAN · SPP Bulanan Rp 400.000 · Kegiatan & Ekskul Rp 75.000 · Total Tagihan Rp 475.000 · Dibayar Rp 0 · Sisa Rp 475.000 · Tutup"`. Screenshot captured inline during session (not persisted to `assets/` — Claude-in-Chrome `save_to_disk` path not recoverable; dialog text above is the authoritative evidence).
- **Suggestion:** Re-run `POST /api/admin/seed` against staging to backfill `xenditPaymentUrl` on existing invoice rows, then add a CI guard (or a pre-`/ship` check) that asserts every demo invoice in non-production environments has a non-null payment URL before the parent UAT can pass.

**Performance**
| Action | Metric | Measured | Threshold | Verdict |
|---|---|---|---|---|
| Navigate to `/parent` (home, first load) | Page full load | 3525 ms | 2.5–4s | major |
| Navigate to `/parent` (reload) | Page full load | 2804 ms | 2.5–4s | major |
| Navigate to `/parent/invoices` | Page full load | 3818 ms | 2.5–4s | major |
| Open invoice detail dialog (click "Lihat") | Click-to-visible | not reliably measurable — MCP round-trip dominates; dialog did render with data populated | <3s | not reported |

---

### JTBD-PARENT-ATT-01 — Check child's attendance this week
- **Persona:** Pak Budi
- **Completed:** yes
- **Severity:** major
- **Observation:** Home "Kehadiran" card correctly shows `"Belum dicatat"` for today (18 Apr 2026, Sabtu — likely a non-school day, which matches). Drilling into `/parent/attendance` shows a flat date/status table: latest row is 14 Apr (Hadir), then 13 Apr (Sakit). As Pak Budi I can count — 14 Apr was Hadir, 13 Apr was Sakit, no entries for 15/16/17 — but the page gives me zero this-week summary (no "X hadir, Y sakit, Z tidak hadir this week" card). For a power-user that's fine; for a parent glancing on a moving train it's one mental step too many and he'll keep scrolling to double-check, which the JTBD explicitly calls out ("Done when: User can count today's status and this week's status without scrolling").
- **Evidence:** `mainText` dump: `"Kehadiran Anak · Tanggal Status · 14 Apr 2026 Hadir · 13 Apr 2026 Sakit · 10 Apr 2026 Hadir · 9 Apr 2026 Hadir · 8 Apr 2026 Hadir · 7 Apr 2026 Hadir ..."`.
- **Suggestion:** Add a small "Minggu ini" summary row above the table ("3 Hadir, 1 Sakit, 0 Tidak Hadir" with StatusBadge icons), calculated server-side from Monday-to-today.

**Performance**
| Action | Metric | Measured | Threshold | Verdict |
|---|---|---|---|---|
| Navigate to `/parent/attendance` | Page full load | 3224 ms | 2.5–4s | major |

---

### JTBD-PARENT-REP-01 — View latest report card
- **Persona:** Pak Budi
- **Completed:** yes
- **Severity:** blocker (timing)
- **Observation:** Once the page lands, the flow is clean — mobile card stack with "Lihat" button (per the 2026-04-17 fix), and tapping it opens a full Laporan Perkembangan dialog with all six domains (NAM, FISMOT, KOGNITIF, BAHASA, SOSEM, SENI) and their BSH/MB/BSB/BB ratings. Emotionally readable, no scroll trap. **But** the list page itself takes 5.3s to reach `loadEventEnd` from a warm staging URL — that's squarely in the blocker band per the PAUD/TKIT thresholds. On a 4G-flaky Commuter train that's well over Pak Budi's "roughly 3 seconds before I assume something broke" threshold from his persona file.
- **Evidence:** `performance.getEntriesByType('navigation')[0].loadEventEnd - startTime = 5259ms`, TTFB 27ms (so the slowness is post-HTML, likely the report-card data fetch inside a Server Component or the published-reports query). Dialog body contains full 6-domain rubric populated from seed.
- **Suggestion:** Profile the `/parent/reports` server component — almost certainly an N+1 on report-card items or an unindexed published-state filter.

**Performance**
| Action | Metric | Measured | Threshold | Verdict |
|---|---|---|---|---|
| Navigate to `/parent/reports` | Page full load | 5259 ms | >4s | blocker |
| Open report dialog (click "Lihat") | Click-to-visible | not reliably measurable — MCP round-trip dominates; dialog did render with all six rubric sections | <3s | not reported |

---

## Heuristic disclaimer
This is synthetic UAT. An LLM persona cannot replicate thumb reach, sunlight
glare, emotional distrust, or network conditions on a 4-year-old phone in
Bekasi traffic. Treat findings as a cheap first pass, not a substitute for
putting the app in front of a real teacher or parent.

## Suggested follow-up

Copy-paste to a new session to kick off a fix cycle:

/spec fix parent cannot pay — invoice dialog shows "link pembayaran sedang disiapkan" with no Bayar CTA on staging

Context: UAT on 2026-04-18 as Pak Budi (live staging session as Ibu Nurul / INV-2026-0201) surfaced a BLOCKER on JTBD-PARENT-INV-01. The invoice detail dialog renders `"Link pembayaran sedang disiapkan. Silakan coba lagi dalam beberapa saat."` and offers only a "Tutup" button — no Bayar action, no alternative payment instructions. This is the same blocker that `docs/uat/jobs/parent.md` marks as resolved on 2026-04-17 in cycle `parent-uat-fixes`; the fix either did not reach staging or the staging DB was never re-seeded so existing invoice rows still have null `xenditPaymentUrl`. Also flag: `/parent/reports` full load is 5.3s on staging (blocker threshold is 4s) — treat as a separate finding.
Full report: docs/uat/reports/2026-04-18-parent.md

Constraints: fix must work for invoices that were seeded *before* the cycle `parent-uat-fixes` ship (backfill path, not just new-invoice path); do not reintroduce the duplicate "sedang disiapkan" message that 2026-04-17 removed; parent payment flow step count must not increase.
