# CRUD Audit — T13: Student Attendance History Tab

## Context
Continuation of the CRUD audit cycle. T01–T12 were completed in `feat/crud-audit` branch.
T13 adds student-level attendance history to the admin student detail page, closing the read gap
for `StudentAttendance` records in the admin portal.

## Spec
- Add "Kehadiran" tab to `/admin/students/[id]` detail page
- Display per-month attendance records with a month filter (default: current month)
- Show summary stat cards: Hadir / Tidak Hadir / Sakit / Izin counts
- Tenant-isolated GET endpoint: `/api/students/[id]/attendance?month=YYYY-MM`

## Tasks
- [x] T13 — Student detail: Attendance history tab

## Implementation
- T13: Student attendance tab — `app/api/students/[id]/attendance/route.ts` (new), `app/admin/students/[id]/page.tsx` (Kehadiran tab added) — per-month filter, 4 stat cards, record list with StatusBadge

## Verification
- T13: gates passed (build ✅, vitest 69/69 ✅)

## Ship Notes
- No DB migrations
- No new env vars
- Smoke test: open any student detail → click Kehadiran tab → verify month filter and records load
