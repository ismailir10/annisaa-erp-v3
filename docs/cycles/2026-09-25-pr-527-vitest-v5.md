# PR #527: Vitest 5 major upgrade

## Context

The user authorized a focused major-version repair for existing PR #527, which upgrades the development test runner `vitest` from 4.1.10 to 5.0.1. Its base is staging commit `5037dac26002d1235dd3c93b361bcba326d1df9b`. The first clean `npm ci` failed with `ERESOLVE`: `@vitest/coverage-v8` remained at 4.1.10, whose peer dependency requires Vitest 4.1.10 exactly. Vitest 5 also no longer supports the repository's Node 20 CI runtime. The CI change to Node 24 and the coverage plugin alignment are part of this repair.

PR #530 separately proposes `@vitest/coverage-v8` 5.0.1. This is coupled to #527 by Vitest's exact peer-version requirement, so #530 is not an independent dependency batch. Keep #530 open until #527 merges and the final lockfile proves its unique dependency changes are included.

## Spec

- Keep the PR limited to the Vitest 5 development-toolchain migration, its compatible coverage provider, the supported Node CI runtime, and setup guidance.
- Align `vitest` and `@vitest/coverage-v8` at 5.0.1 so clean npm resolution satisfies the coverage provider's exact peer dependency.
- Run the full local and required CI gates under Node 24. Keep every required check enabled and preserve the existing E2E skip baseline; introduce no skip changes.
- Treat this as a local developer-tooling change. No app runtime, auth route, session, cookie, or production behavior is intended to change. Reclassify if implementation review finds an app/auth consumer.
- After #527 merges, compare its package manifest and lockfile with all unique #530 changes before closing #530 as superseded.

## Tasks

- [x] Inspect PR #527's Vitest version change and identify its clean-install peer conflict.
- [x] Inspect PR #530's full diff and confirm its coverage-v8 5.0.1 alignment is the required coupling.
- [x] Align coverage-v8 with Vitest 5 and keep CI on Node 24.
- [x] Fix the Vitest DOM matcher setup import exposed by the major-version typecheck.
- [x] Prepare the current-head resolution, required-check, and base/head merge-guard record.
- [x] Run the documentation audit and regenerate the generated counts block.

## Implementation

- Subagent plan: `gpt-6` driver with bounded dependency and compatibility audits delegated to `gpt-6-luna`; the driver reviews the findings and final diff.
- The current local branch is `review/pr-527`, at head `62b1ec9d07fecfdbaa93f4fea04729e4578f84c0`, based on staging `5037dac26002d1235dd3c93b361bcba326d1df9b`. Its tree `acce5401d867844fe79846586250b06670b25fd2` matches the pre-repair candidate tree at `6ca0e7852ccba00684fe126be1a6bec84f2836e7`; Dependabot rebased the source SHA without changing that tree. The working tree aligns `@vitest/coverage-v8` to 5.0.1, sets the three CI jobs to Node 24, and documents Node 24 in README setup instructions.
- PR #527's source changes remain local test-tooling work: Vitest and coverage alignment, the Node 24 CI/setup requirement, and the Vitest matcher setup import. Staging has since advanced to `d8b418ec3a393ff79476bf4794951470de51e63e` after PR #559 merged broad UI and auth-permission changes above the original `5037dac...` base. Those changes belong to the updated base, not PR #527's source delta; review the integrated tree separately before treating the full candidate as dev-tooling-only.
- The read-only PR #530 review found two changed files: `package.json` and `package-lock.json`. Its unique changes are the coverage-v8 5.0.1 manifest/lock entry, matching exact Vitest 5.0.1 peer versions, and the dependency graph changes generated by that bump. The planned coverage alignment should include that coupling; completeness remains unproven until the final PR #527 lockfile is compared after merge.
- This PR updates local test tooling only. No app source, route, or auth implementation changes are part of the intended scope.
- The Vitest 5 typecheck first reported 438 missing DOM matcher errors because `vitest.setup.dom.ts` imported `@testing-library/jest-dom` instead of its Vitest entry point. The setup now imports the documented `@testing-library/jest-dom/vitest`; no assertions were changed, and no casts or suppressions were added. Agent verification reports typecheck passing and 24 focused `status-badge` tests passing.

## Verification

- The initial `npm ci` failed with `ERESOLVE` because Vitest is 5.0.1 while coverage-v8 is still 4.1.10 and declares an exact Vitest 4.1.10 peer. The current candidate lock resolves `vitest` 5.0.1 and `@vitest/coverage-v8` 5.0.1 with the coverage plugin's Vitest peer at 5.0.1; its `@vitest/istanbul-lib-coverage` and `@vitest/istanbul-lib-report` entries are both 1.0.1. After alignment, a clean install completed successfully (`/private/tmp/annisaa-pr-tools/pr527-repaired-install.log`). This confirms the peer-resolution repair; it does not replace the pending test, coverage, typecheck, lint, build, and E2E gates.
- Vitest 5.0.1 declares Node engine support for `^22.12.0 || ^24.0.0 || >=26.0.0`, so Node 20 cannot run the migrated test toolchain. The pre-integration local gates ran on Node 24.19.0, and CI is configured for Node 24. Integrated CI remains pending.
- Before staging integration, the repaired candidate's build and typecheck passed, and lint completed with 0 errors and 59 existing warnings. The final Vitest 5 coverage run passed 3,314 tests across 342 suites, with 2 skipped suites and 42 todo tests; V8 coverage was 71.18% statements, 62.46% branches, 62.03% functions, and 73.41% lines. Logs: `/private/tmp/annisaa-pr-tools/pr527-final-{build,typecheck,lint,coverage}.log`. The focused `status-badge` suite passed all 24 tests after the setup fix.
- Playwright E2E for the pre-integration candidate was intentionally interrupted with exit 130 (SIGINT) in session `90032`, log `/private/tmp/annisaa-pr-tools/pr527-final-e2e.log`: 60 passed, 3 skipped, 1 interrupted, and 89 not run. No failures occurred before it was stopped, but this is not a pass. These pre-integration results do not validate the new staging tree. After the repairs are committed and staging `d8b418e...` is integrated, rerun all gates on that integrated tree and compare the E2E skip count with baseline. The integrated exact-head checks remain pending.
- The generated counts audit passed 13 checks with one existing ADR-age warning and zero failures; `git diff --check` passed. No interactive app/browser flow is needed for this local test-tooling migration. The automated Playwright E2E check remains required and must still be attempted.
- Keep the protected required checks enabled and compare final E2E skips with the existing baseline. Record results against the exact published head; no required CI result is claimed yet.

## Ship Notes

The current pre-integration build, typecheck, lint, and coverage results are recorded above; pre-integration E2E was interrupted and is not a pass. After repairs are committed, integrate staging `d8b418ec3a393ff79476bf4794951470de51e63e` and renew clean install, tests/coverage, typecheck, lint, build, Playwright E2E, skip-baseline comparison, and exact-head required checks. Keep all required CI checks enabled and make no skip-count changes. Keep PR #530 open while PR #527 is pending; after #527 merges, compare the final package manifest and lockfile against all unique #530 changes, then close #530 as superseded only if that comparison confirms they are included. Recheck current base/head before merge. Roll back by reverting the toolchain upgrade commit.
