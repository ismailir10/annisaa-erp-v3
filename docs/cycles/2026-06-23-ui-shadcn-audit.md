# UI Shadcn Audit

## Context

CTO review of the existing UI implementation across admin, finance/HR, curriculum/raport, parent, teacher, public, and shared primitives found no P0 blockers, but several P1 consistency gaps in the surfaces users operate most: list pages that use `DataTable` without controlled search/filter/pagination, raw or pseudo tables where the admin list recipe requires `DataTable`, mobile forms still using desktop-only `Dialog`, stacked overlays in teacher leave, destructive mutations without `AlertDialog`/`ConfirmDialog`, and shared primitives that still permit drift from the Shadcn-first `design-system` standard. Intended outcome: make table/search/filter/form/modal behavior consistent enough that new modules can reuse one clear pattern instead of copying local exceptions.

## Spec

**Acceptance criteria**

- [x] Shared primitives enforce the baseline: `DataTableToolbar` is controlled by parent state (`value` + `onValueChange` + reset affordance), deprecated `components/ui/form-field.tsx` is removed or rebuilt on `Field`, modal close labels use localized `Tutup`, and `ResponsiveFormDialog` remains the default form container.
- [x] Admin list surfaces flagged by review are brought to the Recipe 1 block where row count can exceed 10: `DataTableToolbar`, search, status/domain filters, server-backed pagination where applicable, sortable `DataTableColumnHeader`, standard `DataTableRowActions`, `EmptyState`, and `Skeleton`.
- [x] Finance/HR surfaces close the highest-value gaps: payments ledger gains search/filter/pagination and a view action; payroll-run detail table gains search/filter/pagination and standard view action; shared attendance override modal uses `ResponsiveFormDialog` + `Field`; fee/salary component lists get toolbar/status filtering and remove duplicate lifecycle controls.
- [x] Curriculum/raport surfaces close P1 gaps: admin raport roster moves from raw table to `DataTable`; triwulan create/edit moves from inline card to `ResponsiveFormDialog`; unpublish/retract requires confirm; sentra assessment avoids manual sticky-save when daily-entry optimistic save applies; category/indicator builder gets labeled `Field` rows.
- [x] Portal surfaces close P1 gaps: teacher leave no longer stacks `Sheet` + `Dialog`/`ConfirmDialog`; parent invoice history over 10 rows gets searchable/filterable table behavior; note compose uses responsive dialog/sheet and removes duplicate teacher modal; banned `text-[10px]`/`text-[11px]` portal text is gone.
- [x] Public surfaces align with shared primitives: login card uses Shadcn `Card`, `Button`, `Separator`, `Field`, `Input`, `FieldError`/`Alert`, and `Skeleton`; `/daftar` gender selection uses `RadioGroup` + required field labels.
- [x] Shared formatting/status drift is swept in touched surfaces: `StatusBadge`, `EmptyState`, `formatRupiah`, `formatDate`/`formatDateShort`, and `Progress` replace local badges, plain empty text, inline formatting, and hand-rolled progress bars.
- [x] `design-system` verification is recorded for every frontend task, including relevant sections for DataTable, Forms, Overlays, Portal Shell, and Page Recipes.
- [x] Tests cover primitive behavior and representative module flows: Vitest for controlled toolbar/reset and responsive/modal primitives; Playwright or component smoke for at least admin payments, admin raport, teacher leave, parent invoices, and public login.

**Non-goals**

- Redesigning visual identity, navigation IA, or color system beyond enforcing existing tokens and primitives.
- Solving data/business findings from UAT that are not UI component parity, such as multiple active academic years, staging fixture pollution, Xendit link failures, or admission source mutation.
- Rewriting every small static card list. Card lists under 10 items may remain if they have explicit empty/loading/error states and no table affordance is needed.
- Adding new Shadcn packages or alternate UI libraries.
- Changing API semantics except where server pagination/search/filter is required for an existing list.

**Review inputs**

- GPT-5.4 subagents reviewed admin core, curriculum/raport, finance/HR, teacher/parent portals, and public/shared primitives.
- Local grep review covered `DataTable`, `DataTableToolbar`, `DataTableRowActions`, `ResponsiveFormDialog`, raw `table`, raw `Label`, `window.confirm`, `animate-pulse`, inline formatting, banned portal text, hardcoded colors, and overlay usage.
- Chrome MCP staging smoke (2026-06-23, admin session on `annisaa-erp-v3-git-staging-ismails-projects-196d40d3.vercel.app`) verified representative findings: payments ledger has date/method filters but no search/pagination/view action; academic-years and fees have table/list gaps; salary components has 13 rows with no search/status filter and visible English `Close` in the dialog; student-journal monitoring shows one-page fabricated pagination; monthly attendance renders a dense 28-row raw grid.
- Chrome MCP staging smoke with the teacher account verified `/teacher/attendance`: `Cuti & Izin` is not exposed as a keyboard/accessibility action in the visible tree, opening it by visual click shows English `Close`, and `Ajukan Cuti` creates two simultaneous visible `role="dialog"` overlays (`Sheet` + nested form dialog).
- Chrome MCP staging smoke with the parent account verified `/parent/invoices`: 15 invoice/payment rows render as card-like buttons, with zero tables, zero search inputs, and no status/search/filter/pagination affordance despite 8 unpaid and 7 payment-history items.
- Fresh UAT input from `docs/uat/reports/2026-06-04-admin-teacher-full.md` contributed teacher assessment/empty-state and admin workflow context.
- Recent parent/teacher UAT reports from `docs/uat/reports/2026-04-25-parent.md` and `docs/uat/reports/2026-05-03-teacher.md` were read for portal context; findings are possibly stale where later cycles changed the same files.

**Assumptions** (correct now or `/build` proceeds with these)

1. Treat `app/admin/students/page.tsx`, `app/admin/guardians/page.tsx`, `app/admin/admissions/page.tsx`, `app/admin/invoices/page.tsx`, `app/admin/(hr)/employees/page.tsx`, `app/admin/assessment-templates/page.tsx`, and `app/teacher/class-attendance/page.tsx` as reference patterns to preserve and reuse.
2. Server-backed pagination/search is required for operational lists likely to grow, but small fixed configuration lists may keep client-side data if they still use the canonical toolbar/filter/action shape.
3. Portal long lists can use `DataTable` if the table is responsive and mobile-readable; if implementation proves poor on 375 px, a portal-specific table/list hybrid must still expose search/filter/sort equivalents.
4. `ConfirmDialog` is acceptable for destructive confirmation because it wraps `AlertDialog`; raw destructive `Dialog` submit is not.
5. This is one remediation cycle with prioritized slices. Lower-risk P2 polish not touched by these tasks becomes follow-up debt, not a blocker.

## Tasks

1. [x] **[T1] Shared primitive baseline** — update `DataTableToolbar` to controlled state with reset, remove/rebuild deprecated `FormField`, localize modal close copy, and add/adjust primitive tests. Acceptance: existing admin lists still compile; toolbar query can be reset from parent state; `rg "from \"@/components/ui/form-field\"" app components` returns no live consumers or only the rebuilt primitive.

2. [x] **[T2] Admin list parity sweep** — apply the missing pieces of the canonical toolbar/search/filter/action/pagination shape to highest-impact admin-core gaps: classes list (preserve existing search/filters; add missing pagination/server contract if needed), semesters list, academic-years program/year lists, student-attendance monthly recap, student-journal class roll-up, and monitoring pagination. Acceptance: each touched list has explicit search/filter behavior where useful, proper empty/loading states, sortable headers, and no fake pagination.

3. [x] **[T3] Finance/HR parity sweep** — fix payments ledger, payroll-run detail employee table, attendance override modal, fee components, salary components, monthly attendance empty state, and invoice batch progress. Acceptance: payments/payroll tables are operable with search/filter/pagination; override modal is responsive and `Field`-based; config lifecycle actions use one standard row action path; progress uses Shadcn `Progress`.

4. [x] **[T4] Curriculum and raport sweep** — convert admin raport raw table to `DataTable`, move triwulan create/edit into `ResponsiveFormDialog`, confirm unpublish/retract, label category/indicator builder fields, and remove banned/raw controls in sentra assessment. Acceptance: no raw admin raport table; no inline triwulan create card; sentra UI matches daily-entry or has documented exception; `rg "text-\\[11px\\]" app/teacher components/teacher components/portal components/student-journal` returns no active portal UI hits.

5. [x] **[T5] Portal flow sweep** — remove teacher leave overlay stacking, make note compose responsive/shared, add searchable/filterable parent invoice history for long lists, normalize portal dashboard header/quick-link recipe where low-risk, and sweep banned portal microtext/date formatting. Acceptance: one overlay open at a time in teacher leave; parent invoice history has search/filter/sort affordance for >10 rows; note compose uses one shared responsive component.

6. [x] **[T6] Public form/auth sweep** — rebuild public login card with Shadcn primitives and migrate `/daftar` gender/required fields to shared form primitives. Acceptance: login states use `Button`, `Input`, `Field`, `Alert`/`FieldError`, `Skeleton`, and `Separator`; `/daftar` gender uses `RadioGroup`; visible behavior unchanged.

7. [x] **[T7] Verification and docs** — run targeted Vitest, `npm run build`, targeted Playwright/component smoke for admin payments, admin raport, teacher leave, parent invoices, and login; update README/CLAUDE only if route/module counts or standards change; record `design-system` cross-checks in Verification. Acceptance: gates green or documented with exact blocker; no unrelated markdown files created.

## Implementation

- Subagent plan: all tasks sequential for now. T1 changes shared primitives used by T2-T6; after T1 stabilizes, later module sweeps may use bounded reviewer/worker subagents only when write sets are disjoint.
- Task 1: Shared primitive baseline — `components/ui/data-table-toolbar.tsx`, `components/ui/form-field.tsx`, `components/ui/dialog.tsx`, `components/ui/sheet.tsx`, primitive tests, and narrow Next 16 gate unblockers in API/page helper exports — made toolbar search parent-controllable with reset, rebuilt legacy `FormField` on `Field`, localized default close copy to `Tutup`, and moved test-only/page helper exports out of restricted route/page module exports.
- Task 2: Admin list parity sweep — `app/admin/classes/client.tsx`, `app/admin/semesters/client.tsx`, `app/admin/academic-years/page.tsx`, `app/admin/student-attendance/page.tsx`, `app/admin/student-journal/monitoring/page.tsx`, `app/admin/student-journal/classes/[id]/page.tsx`, `components/ui/data-table.tsx`, `components/ui/data-table-pagination.tsx` — added canonical toolbar search/filter and real client pagination to bounded admin lists, removed student-journal fake pagination, converted the class roll-up pseudo-table to `DataTable`, and made client-side DataTable pagination sort the full filtered dataset before paging.
- Task 3: Finance/HR parity sweep — `app/admin/payments/page.tsx`, `app/api/payments/route.ts`, `app/api/payments/export/route.ts`, `lib/finance/payments-ledger.ts`, `app/admin/(hr)/payroll/[id]/page.tsx`, `components/attendance/override-modal.tsx`, `app/admin/fees/page.tsx`, `app/admin/(hr)/salary-components/page.tsx`, `app/admin/(hr)/employee-attendance/monthly/page.tsx`, `components/admin/invoices/batch-progress-card.tsx`, and focused payments tests — added server-backed payments search/filter/sort/pagination with invoice view actions, standardized payroll/config list toolbars and row actions, rebuilt the attendance override modal on responsive `Field` primitives, added the monthly attendance empty state, and replaced invoice batch progress bars with Shadcn `Progress`.
- Task 4: Curriculum and raport sweep — `app/admin/raport/page.tsx`, `app/admin/raport/raport-editor.tsx`, `components/admin/assessments/KategoriIndikatorBuilder.tsx`, `app/teacher/assessments/center/[center]/client.tsx`, `components/student-journal/class-day-grid.tsx` — moved the raport roster to `DataTable` with toolbar search/status filter and client pagination, replaced inline triwulan create/edit with `ResponsiveFormDialog`, added `AlertDialog` confirmation for retracting published raport, labeled category/indicator builder rows with `Field`, and removed the active portal banned microtext hits. Sentra remains a documented daily-entry exception with explicit manual save because center sessions batch a shared activity plus multiple IKTP/student cells.
- Task 5: Portal flow sweep — `components/teacher/leave-sheet.tsx`, `components/student-journal/note-compose-dialog.tsx`, `app/teacher/student-journal/students/[id]/page.tsx`, and `app/parent/invoices/client.tsx` — moved teacher leave request into `ResponsiveFormDialog`, closed the sheet before opening request/cancel overlays so only one overlay is visible, removed the duplicate teacher-local note modal in favor of the shared responsive note composer, and added search/status/sort/reset affordances plus a filter-empty state for parent invoice lists over 10 rows. Portal dashboard header/quick-link recipes were left unchanged because no low-risk touched dashboard surface needed adjustment in this slice.
- Task 6: Public form/auth sweep — `app/page.tsx` and `app/daftar/client.tsx` — rebuilt the public login auth card on Shadcn `Card`, `Button`, `Input`, `Field`, `Alert`, `Skeleton`, and `Separator` primitives while preserving Supabase/demo-login behavior, and migrated `/daftar` gender selection to `RadioGroup`/`RadioGroupItem` with shared required field labels for required admission fields.
- Task 7: Verification and docs — `components/teacher/__tests__/leave-sheet.test.tsx`, `app/parent/invoices/__tests__/client.test.tsx`, and this cycle doc — added focused component smokes for the mobile teacher leave overlay handoff and parent long-list search/status/sort/reset controls, then recorded final targeted Vitest, production build, local Playwright smoke, design-system cross-checks, and staging Chrome sanity results. README/CLAUDE were not changed because route/module counts and standards did not change.

## Verification

- Task 1: gates passed. Design-system cross-check: Forms, DataTable, and Overlays sections. Focused Vitest passed for toolbar/form-field/overlay primitives, campus route, Xendit health route, teacher slip helpers, and student-journal date helper. Production build passed via `prisma generate` + `next build --webpack` with local env loaded; default Turbopack build is blocked in this worktree by the existing `node_modules` symlink layout. Full Vitest passed: 210 files passed, 2 skipped; 2067 tests passed, 42 todo.
- Task 2: gates passed. Design-system cross-check: DataTable and Page Recipes sections. `rg` verified no fake pagination/no-op pagination remains in touched student-journal/admin list surfaces. Focused DataTable regression test covers global sort before client pagination. Production build passed via `prisma generate` + `next build --webpack` with local env loaded. Full Vitest passed: 211 files passed, 2 skipped; 2068 tests passed, 42 todo.
- Task 3: gates passed. Design-system cross-check: DataTable, Forms, Overlays, Page Recipes, Progress, and Empty/Loading/Error sections. Focused Vitest passed for payments ledger search/pagination/sort validation and payments auth/export role+permission guards: 31 tests passed. Production build passed via `prisma generate` + `next build --webpack` with local env loaded. Full Vitest passed: 212 files passed, 2 skipped; 2077 tests passed, 42 todo. Reviewer findings on hook ordering, pagination validation, stable ordering, guardian access, stale payroll details, payments error clearing, and salary checkbox labeling were fixed before commit.
- Task 4: gates passed. Design-system cross-check: DataTable, Forms, Overlays, Page Recipes, Portal Shell, and Empty/Loading/Error sections. `rg "text-\\[10px\\]|text-\\[11px\\]" app/teacher components/teacher components/portal components/student-journal` returned no active hits; `rg "<table|</table>" app/admin/raport app/teacher/assessments components/admin/assessments` returned no hits. Production build passed via `prisma generate` + `next build --webpack` with local env loaded. Full Vitest passed: 212 files passed, 2 skipped; 2077 tests passed, 42 todo. Reviewer finding on the triwulan edit dialog advertising an unsupported semester move was fixed by disabling Semester in edit mode before commit.
- Task 5: gates passed. Design-system cross-check: Forms, Overlays, Portal Shell, Page Recipes, and Empty/Loading/Error sections. `rg "text-\\[10px\\]|text-\\[11px\\]" app/parent app/teacher components/parent components/teacher components/portal components/student-journal` returned no active hits; focused `rg` verified changed leave/note files no longer import raw `Dialog` primitives. Production build passed via `prisma generate` + `next build --webpack` with local env loaded. Full Vitest passed: 212 files passed, 2 skipped; 2077 tests passed, 42 todo.
- Task 6: gates passed. Design-system cross-check: Forms, Public/Auth, Empty/Loading/Error, and Page Recipes sections. `rg "<button|<input|bg-login-card-bg rounded|h-14 rounded-xl bg-white/5 opacity|role=\"radiogroup\"|type=\"radio\"" app/page.tsx app/daftar/client.tsx` returned no hits. Production build passed via `prisma generate` + `next build --webpack` with local env loaded. Full Vitest passed: 212 files passed, 2 skipped; 2077 tests passed, 42 todo.
- Task 7: gates passed with one documented stock-spec blocker. Design-system cross-check: DataTable, Forms, Overlays, Portal Shell, Page Recipes, Public/Auth, Progress, and Empty/Loading/Error sections. Targeted Vitest passed: 12 files passed, 102 tests passed (`DataTableToolbar`, rebuilt `FormField`, overlay close labels, `DataTable`, parent invoices long-list controls, teacher leave mobile overlay handoff, payments ledger/auth, admin raport route, leave submit/cancel APIs, and note-date helper). Production build passed via `prisma generate` + `next build --webpack` with local env loaded; expected Next.js dynamic server/cookies warnings appeared, and the route manifest finalized successfully. Read-only local Playwright smoke against the built server passed for final branch behavior: dynamic admin-user discovery opened `/admin/payments` and verified the post-remediation search input (`Cari siswa, tagihan, referensi...`), reset action, method filter, and `EmptyState`; opened `/admin/raport` and verified the triwulan/class selector plus `Edit Triwulan` entry without raw table regressions before selection; existing `e2e/branding.spec.ts` passed 5/5, including the login screen. The stock `e2e/admin-payments.spec.ts` and `e2e/admin-raport.spec.ts` were attempted but blocked because they hardcode `u_super_admin`, which returned 401/redirect on the current staging-backed demo DB; the replacement smoke used the same dynamic user discovery pattern as `branding.spec.ts`. Chrome MCP staging smoke used the Vercel share URL for `annisaa-erp-v3-git-staging-ismails-projects-196d40d3.vercel.app` as a sanity check of the original findings, not as final branch verification: public login still showed the pre-remediation raw login card (`bg-login-card-bg rounded-2xl`, raw button/input), admin `/admin/payments` still lacked search/pagination/view action, and admin `/admin/raport` still lacked table/search/pagination before selection, confirming the spec findings made sense on deployed staging. Teacher `/teacher/attendance` and parent `/parent/invoices` could not be re-smoked on staging in this session because Chrome was actively authenticated as admin, those routes redirected to `/admin`, the demo user endpoint is unavailable/blocked in production-mode staging, and the provided teacher/parent emails require an OAuth account switch rather than a credential-less API switch; final branch coverage for those two surfaces is via local component smokes.

## Ship Notes
